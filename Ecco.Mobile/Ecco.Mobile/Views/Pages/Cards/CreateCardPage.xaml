<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Ecco.Mobile.Views.Pages.CreateCardPage"
             Title="Create Card"
             xmlns:viewmodels="clr-namespace:Ecco.Mobile.ViewModels.Home"
             xmlns:syncfusion="clr-namespace:Syncfusion.XForms.Expander;assembly=Syncfusion.Expander.XForms"
             xmlns:xforms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms" 
             xmlns:local="clr-namespace:Ecco.Mobile.Views.Authentication" xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms" xmlns:border="clr-namespace:Syncfusion.XForms.Border;assembly=Syncfusion.Core.XForms" xmlns:control="clr-namespace:Ecco.Mobile.Controls" xmlns:converter="clr-namespace:Ecco.Mobile.Converters"
             BackgroundColor="Blue">

    <ContentPage.BindingContext>
        <viewmodels:CreateCardViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:BooleanToColorConverter x:Key="ColorConverter" />
            <ResourceDictionary.MergedDictionaries>
                <local:Styles/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout>

            <Entry Placeholder="Card Title" Style="{StaticResource EntryStyle}" Text="{Binding CardTitle}"/>

            <Entry Placeholder="Job Title" Style="{StaticResource EntryStyle}" Text="{Binding JobTitle}"/>

            <Entry Placeholder="Description" Style="{StaticResource EntryStyle}" Text="{Binding Description}"/>

            <Entry Placeholder="Email" Style="{StaticResource EntryStyle}" Text="{Binding Email}"/>

            <Entry Placeholder="Phone Number" Style="{StaticResource EntryStyle}" Text="{Binding Phone}"/>

            <!--  Entry to get password from user  -->
            <border:SfBorder
                            BorderColor="{Binding Source={x:Reference entryText}, Path=IsFocused, Converter={StaticResource ColorConverter}, ConverterParameter=0}"
                            Style="{StaticResource SfBorderStyle}">
                <control:BorderlessEntry
                                x:Name="entryText"
                                Placeholder="Text"
                                Style="{StaticResource EntryStyle}" />
            </border:SfBorder>

            <ScrollView BackgroundColor="#EDF2F5" Grid.Row="1">
                <StackLayout>
                    
                    <syncfusion:SfExpander>
                        <syncfusion:SfExpander.Header>
                        
                            <Grid>
                                <Label TextColor="#495F6E" Text="Choose A Template" VerticalTextAlignment="Center" />
                            </Grid>
                    
                        </syncfusion:SfExpander.Header>
                        
                        <syncfusion:SfExpander.Content>
                            
                            <StackLayout Padding="10,10,10,10" BackgroundColor="#FFFFFF">

                                <xforms:SfListView ItemsSource="{Binding Templates}"
                                                   ItemSize="200"
                                                   x:Name="ListTemplates"
                                                   ItemTapped="SfListView_ItemTapped">

                                    <xforms:SfListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackLayout>
                                                <Label Text="{Binding Template.Title}"
                                                       HorizontalOptions="Center"/>
                                                <Image Source="{Binding TemplateImage}"
                                                       Aspect="AspectFit">

                                                    <Image.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding TemplateSelectedCommand}"
                                                                              CommandParameter="{Binding .}"/>
                                                    </Image.GestureRecognizers>
                                                    
                                                </Image>

                                            </StackLayout>
                                        </DataTemplate>
                                    </xforms:SfListView.ItemTemplate>
                                    
                                </xforms:SfListView>
                            
                            </StackLayout>
                            
                        </syncfusion:SfExpander.Content>
                    </syncfusion:SfExpander>
                </StackLayout>
            </ScrollView>

            <buttons:SfButton Text="Create" 
                    HorizontalOptions="Center"
                    Command="{Binding CreateCommand}"
                    Style="{StaticResource SfFlatButtonStyle}"/>

        </StackLayout>
  </ContentPage.Content>
</ContentPage>